export function divide(x, y) {
    if (y !== 0) {
        var result = x / y;
    }
    return result;
}

export function divideSecondImplementation(x, y) {
    if (y !== 0) {
        let result = x / y;
    }
    
    return result;
}